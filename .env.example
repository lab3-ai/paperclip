DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false

# Required in authenticated mode — superadmin credentials seeded on first startup
PAPERCLIP_ADMIN_EMAIL=admin@yourcompany.com
PAPERCLIP_ADMIN_PASSWORD=change-me-on-first-login
